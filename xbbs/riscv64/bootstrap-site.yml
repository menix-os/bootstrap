define_options:
  arch: riscv64

labels:
  match:
  - riscv64
  - any
  ban:
  - local-only

pkg_management:
  format: xbps

container:
  runtime: cbuildrt
  rootfs: /var/lib/cbuildrt/menix-buildenv
  uid: 1000
  gid: 1000
  src_mount: /var/lib/menix-buildenv/src
  build_mount: /var/lib/menix-buildenv/build-riscv64
  allow_containerless: true
