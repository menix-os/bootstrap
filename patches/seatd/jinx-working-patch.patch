diff --git seatd-clean/common/drm.c seatd-workdir/common/drm.c
index 9591dc0..8096512 100644
--- seatd-clean/common/drm.c
+++ seatd-workdir/common/drm.c
@@ -34,7 +34,7 @@ int path_is_drm(const char *path) {
 int dev_is_drm(dev_t device) {
 	return major(device) == 226;
 }
-#elif defined(__FreeBSD__)
+#elif defined(__FreeBSD__) || defined(__zinnia__)
 int path_is_drm(const char *path) {
 	static const char prefix[] = "/dev/drm/";
 	static const int prefixlen = STRLEN(prefix);
diff --git seatd-clean/common/evdev.c seatd-workdir/common/evdev.c
index 4aff9bc..f3dd4fe 100644
--- seatd-clean/common/evdev.c
+++ seatd-workdir/common/evdev.c
@@ -9,6 +9,8 @@
 #include <sys/sysmacros.h>
 #elif defined(__FreeBSD__)
 #include <dev/evdev/input.h>
+#elif defined(__zinnia__)
+#include <linux/input.h>
 #else
 #error Unsupported platform
 #endif
diff --git seatd-clean/common/terminal.c seatd-workdir/common/terminal.c
index 0c3466f..cb431f3 100644
--- seatd-clean/common/terminal.c
+++ seatd-workdir/common/terminal.c
@@ -21,6 +21,8 @@
 #define K_ENABLE  K_XLATE
 #define K_DISABLE K_RAW
 #define FRSIG     SIGIO
+#elif defined(__zinnia__)
+// TODO: Add VT support
 #else
 #error Unsupported platform
 #endif
@@ -134,6 +136,14 @@ static int get_tty_path(int tty, char path[static TTYPATHLEN]) {
 	}
 	return 0;
 }
+#elif defined(__zinnia__)
+static int get_tty_path(int tty, char path[static TTYPATHLEN]) {
+	(void)tty;
+	// Zinnia has no VTs; open /dev/null so the fd is valid but all
+	// VT ioctls (stubbed as no-ops below) don't need a real terminal.
+	strcpy(path, "/dev/null");
+	return 0;
+}
 #else
 #error Unsupported platform
 #endif
@@ -176,6 +186,9 @@ int terminal_current_vt(int fd) {
 		return -1;
 	}
 	return vt;
+#elif defined(__zinnia__)
+	(void)fd;
+	return 0;
 #else
 #error Unsupported platform
 #endif
@@ -183,6 +196,7 @@ int terminal_current_vt(int fd) {
 
 int terminal_set_process_switching(int fd, bool enable) {
 	log_debugf("Setting process switching to %d", enable);
+#if !defined(__zinnia__)
 	struct vt_mode mode = {
 		.mode = enable ? VT_PROCESS : VT_AUTO,
 		.waitv = 0,
@@ -196,41 +210,50 @@ int terminal_set_process_switching(int fd, bool enable) {
 			   enable ? "enable" : "disable", strerror(errno));
 		return -1;
 	}
+#endif
+	(void)fd;
 	return 0;
 }
 
 int terminal_switch_vt(int fd, int vt) {
 	log_debugf("Switching to VT %d", vt);
+#if !defined(__zinnia__)
 	if (ioctl(fd, VT_ACTIVATE, vt) == -1) {
 		log_errorf("Could not activate VT %d: %s", vt, strerror(errno));
 		return -1;
 	}
-
+#endif
+	(void)fd;
 	return 0;
 }
 
 int terminal_ack_release(int fd) {
 	log_debug("Acking VT release");
+#if !defined(__zinnia__)
 	if (ioctl(fd, VT_RELDISP, 1) == -1) {
 		log_errorf("Could not ack VT release: %s", strerror(errno));
 		return -1;
 	}
-
+#endif
+	(void)fd;
 	return 0;
 }
 
 int terminal_ack_acquire(int fd) {
 	log_debug("Acking VT acquire");
+#if !defined(__zinnia__)
 	if (ioctl(fd, VT_RELDISP, VT_ACKACQ) == -1) {
 		log_errorf("Could not ack VT acquire: %s", strerror(errno));
 		return -1;
 	}
-
+#endif
+	(void)fd;
 	return 0;
 }
 
 int terminal_set_keyboard(int fd, bool enable) {
 	log_debugf("Setting KD keyboard state to %d", enable);
+#if !defined(__zinnia__)
 	if (ioctl(fd, KDSKBMODE, enable ? K_ENABLE : K_DISABLE) == -1) {
 		log_errorf("Could not set KD keyboard mode to %s: %s",
 			   enable ? "enabled" : "disabled", strerror(errno));
@@ -253,15 +276,20 @@ int terminal_set_keyboard(int fd, bool enable) {
 		return -1;
 	}
 #endif
+#endif
+	(void)fd;
 	return 0;
 }
 
 int terminal_set_graphics(int fd, bool enable) {
 	log_debugf("Setting KD graphics state to %d", enable);
+#if !defined(__zinnia__)
 	if (ioctl(fd, KDSETMODE, enable ? KD_GRAPHICS : KD_TEXT) == -1) {
 		log_errorf("Could not set KD graphics mode to %s: %s", enable ? "graphics" : "text",
 			   strerror(errno));
 		return -1;
 	}
+#endif
+	(void)fd;
 	return 0;
 }
diff --git seatd-clean/seatd/client.c seatd-workdir/seatd/client.c
index 220c5d3..df57c42 100644
--- seatd-clean/seatd/client.c
+++ seatd-workdir/seatd/client.c
@@ -24,7 +24,7 @@
 #include "terminal.h"
 
 static int get_peer(int fd, pid_t *pid, uid_t *uid, gid_t *gid) {
-#if defined(__linux__)
+#if defined(__linux__) || defined(__zinnia__)
 	struct ucred cred;
 	socklen_t len = sizeof cred;
 	if (getsockopt(fd, SOL_SOCKET, SO_PEERCRED, &cred, &len) == -1) {
